# 1) Limpiar restos anteriores
sudo apt-get remove --purge -y docker docker-engine docker.io docker-ce docker-ce-cli containerd runc || true
sudo apt-get autoremove -y
sudo apt-get update

# 2) Ejecutar el instalador oficial automático
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 3) Arrancar y habilitar el servicio
sudo systemctl enable --now docker

# 4) Comprobar estado
sudo systemctl status docker --no-pager
docker --version
docker compose version || true